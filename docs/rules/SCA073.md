# SCA073 — Empty interface declaration

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Interface with no methods/properties carries no contract

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA073` |
| **Kind** | `EmptyInterface` |
| **Default severity** | Hint |
| **Type** | Code Smell |
| **Detector unit** | `uEmptyInterface.pas` |
| **Tags** | `api-design`, `dead-code` |

## Description

An IFoo = interface end; body without any methods or properties is either a refactor leftover or a marker interface in disguise. Marker interfaces are better modeled as attribute classes in Delphi. Matches SonarDelphi communitydelphi:EmptyInterface. Not reported: an interface with a *named* ancestor other than IInterface/IUnknown. It inherits the ancestor's contract and serves as a query key (Obj as IFileSystemFileSource), a marker derivation (IOTAFormWizard = interface(IOTARepositoryWizard)), an ObjC/JNI bridge type or a *_TLB.pas automation type - the suggested attribute-class fix works for none of them, because attributes are compile-time RTTI and cannot be queried via QueryInterface. interface(IUnknown) is still reported: it is semantically identical to interface.

## Examples

### Bad (triggers the rule)

```pascal
type IServiceMarker = interface end;
```

### Good (idiomatic fix)

```pascal
type ServiceMarkerAttribute = class(TCustomAttribute) end;
```

## Not reported (by design)

The detector deliberately stays silent on the constructs below. Each is a false-positive class that was measured on the real-world corpus, not a guess.

### Interface with a named ancestor other than IInterface/IUnknown

The rule's premise - 'carries no contract' - is false here: the interface inherits the ancestor's members and exists as a query key (`Obj as IFileSystemFileSource`), as an Embarcadero-style marker derivation (`IOTAFormWizard = interface(IOTARepositoryWizard)`), as an ObjC/JNI bridge type, or as a `*_TLB.pas` automation type generated from a type library. The suggested fix - model it as an attribute class - does not work for any of them, because attributes are compile-time RTTI and cannot be queried through `QueryInterface`. `IInterface`/`IUnknown` are NOT exempt: `IFoo = interface(IUnknown) end;` is semantically identical to `IFoo = interface end;`, since IInterface is the implicit default ancestor.

```pascal
// not reported - refines an existing contract
IFileSystemFileSource = interface(IFileSource)
  ['{4CBD5A32-...}']
end;

// still reported - no ancestor contract to inherit
IServiceMarker = interface(IUnknown)
end;
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
