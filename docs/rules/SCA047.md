# SCA047 — x := x

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> Self-assignment - no-op or copy-paste typo

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA047` |
| **Kind** | `SelfAssignment` |
| **Default severity** | Warning |
| **Type** | Bug |
| **Detector unit** | `uSelfAssignment.pas` |
| **Tags** | `typo`, `no-op` |

## Description

Reports `x := x` only when the target is provably a storage slot: a local variable, a parameter, `Result`, an assignment to the function name, or a field declared in the same unit. Property targets, member paths (`a.b`, `p^.f`, `a[i]`) and names that cannot be resolved inside the unit are NOT reported - writing to a property runs its setter, which may clamp, re-apply or reformat the value (`TopLine := TopLine` clamps against the resized window; `Text := Text` re-parses). The trade-off is deliberate: unknown forms become false negatives, never false positives.

## Examples

### Bad (triggers the rule)

```pascal
FCount := FCount;
```

### Good (idiomatic fix)

```pascal
FCount := ACount;
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
