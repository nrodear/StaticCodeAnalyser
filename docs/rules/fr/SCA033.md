# SCA033 — Propriété SQL construite depuis une saisie d'interface

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Vulnerability-darkred)

> Query.SQL assemblée à partir des propriétés Text ou Caption de contrôles de fiche

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA033` |
| **Kind** | `DfmSqlFromUserInput` |
| **Sévérité par défaut** | Error |
| **Type** | Vulnerability |
| **Unité du détecteur** | `uDfmSqlFromUserInput.pas` |
| **Étiquettes** | `dfm`, `sql`, `injection`, `security` |
| **CWE** | [CWE-89](https://cwe.mitre.org/data/definitions/89.html) |
| **OWASP** | A03:2021-Injection |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

A DB-query SQL property (TADOQuery / TFDQuery / TUniQuery / TZQuery / TKSQLQuery.SQL.Text) is assembled by concatenating the Text / Caption / Hint / EditText / ItemText property of a UI input control (TEdit, TComboBox, TMaskEdit, ...) - classic SQL injection through the form. A user typing `' OR ''1''=''1' --` into the search field reads any row; `'; DROP TABLE Users; --` is destructive. Refactor: bind named parameters and assign via ParamByName, never concatenate. This detector is the DFM/UI-path companion to SCA003 (lexical SQL injection) and SCA058 (dangerous SQL statement).

## Exemples

### Mauvais (déclenche la règle)

```pascal
FDQuery1.SQL.Text := 'SELECT * FROM U WHERE Name=''' + EdName.Text + '''';
```

### Bon (correction idiomatique)

```pascal
FDQuery1.SQL.Text := 'SELECT * FROM U WHERE Name=:n';
FDQuery1.ParamByName('n').AsString := EdName.Text;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
