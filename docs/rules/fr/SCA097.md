# SCA097 — Destructeur sans appel a inherited

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Bug-red)

> Il manque `inherited Destroy` au destructeur - le nettoyage du parent est sauté (risque de fuite)

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA097` |
| **Kind** | `DestructorWithoutInherited` |
| **Sévérité par défaut** | Error |
| **Type** | Bug |
| **Unité du détecteur** | `uDestructorWithoutInherited.pas` |
| **Étiquettes** | `inheritance`, `memory-leak` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

A destructor without `inherited Destroy` (or `inherited;`) prevents the parent class from releasing its own fields, unregistering handlers, and updating reference counts. Almost always a leak. Matches SonarDelphi communitydelphi:DestructorWithoutInherited.

## Exemples

### Mauvais (déclenche la règle)

```pascal
destructor TFoo.Destroy;
begin
  FreeAndNil(FBar);
end;
```

### Bon (correction idiomatique)

```pascal
destructor TFoo.Destroy;
begin
  FreeAndNil(FBar);
  inherited;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
