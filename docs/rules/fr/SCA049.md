# SCA049 — Length(s) - N sans garde

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Bug-red)

> Length ou .Count avec soustraction - dépassement par le bas du type non signe quand c'est vide

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA049` |
| **Kind** | `LengthUnderflow` |
| **Sévérité par défaut** | Hint |
| **Type** | Bug |
| **Unité du détecteur** | `uLengthUnderflow.pas` |
| **Étiquettes** | `arithmetic`, `underflow` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

`Length(s) - 1` on an empty string evaluates to 0 - 1 = MaxUInt under NativeUInt arithmetic and indexes into garbage. Guard for emptiness or cast to NativeInt.

## Exemples

### Mauvais (déclenche la règle)

```pascal
Last := s[Length(s) - 1];
```

### Bon (correction idiomatique)

```pascal
if Length(s) >= 2 then Last := s[Length(s) - 1];
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
