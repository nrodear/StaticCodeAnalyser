# SCA045 — with X do ...

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Instruction with - piège de masquage de portée contre lequel le compilateur n'avertit pas

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA045` |
| **Kind** | `WithStatement` |
| **Sévérité par défaut** | Warning |
| **Type** | Code Smell |
| **Unité du détecteur** | `uWithStatement.pas` |
| **Étiquettes** | `scope`, `delphi-classic` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Marco Cantu, delphi.org and Stack Overflow consistently rank `with` among the top Delphi bug sources. Identifiers from the outer scope get silently shadowed by members of the with-target. Use a local variable alias instead.

## Exemples

### Mauvais (déclenche la règle)

```pascal
with Customer do
begin
  Name := SomeName;   // Customer.Name? or local Name?
end;
```

### Bon (correction idiomatique)

```pascal
C := Customer;
C.Name := SomeName;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
