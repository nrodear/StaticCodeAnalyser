# SCA099 — begin/end asymétrique dans if/else

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> La branche then utilise `begin..end` mais la branche else est une instruction unique

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA099` |
| **Kind** | `IfElseBegin` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uIfElseBegin.pas` |
| **Étiquettes** | `style`, `consistency` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

When the then-branch uses `begin..end`, the else-branch should too (or both should drop it). The mixed form (`end else DoStuff;`) makes a future reader unsure whether more statements belong in the else-branch but were forgotten. `else if` (else-if chain) and `else begin` are explicitly allowed. Matches SonarDelphi communitydelphi:IfElseBegin.

## Exemples

### Mauvais (déclenche la règle)

```pascal
if Active then
begin
  DoA;
  DoB;
end
else
  DoC;
```

### Bon (correction idiomatique)

```pascal
if Active then
begin
  DoA;
  DoB;
end
else
begin
  DoC;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
