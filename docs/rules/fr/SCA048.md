# SCA048 — Appel virtuel dans le constructeur

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Bug-red)

> Méthode virtuelle appelée depuis le constructeur - la classe dérivée voit un Self à moitié initialisé

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA048` |
| **Kind** | `VirtualCallInCtor` |
| **Sévérité par défaut** | Error |
| **Type** | Bug |
| **Unité du détecteur** | `uVirtualCallInCtor.pas` |
| **Étiquettes** | `oop`, `initialization-order` |
| **CWE** | [CWE-665](https://cwe.mitre.org/data/definitions/665.html) |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

C++ FAQ 23.5 / *Effective Java* item 17 in Delphi form: virtual dispatch in a constructor runs the most-derived override before subclass fields are initialised. Defer to a non-virtual post-construction hook.

## Exemples

### Mauvais (déclenche la règle)

```pascal
constructor TBase.Create;
begin
  Configure;   // virtual
end;
```

### Bon (correction idiomatique)

```pascal
constructor TBase.Create;
begin
  // skip virtual call
end;
procedure TBase.AfterConstruction;
begin
  Configure;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
