# SCA109 — Verrou pris sans libération par try/finally

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Bug-red)

> TCriticalSection, Monitor ou verrou WinAPI pris sans try..finally englobant - une exception laisse le verrou posé

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA109` |
| **Kind** | `LockWithoutTryFinally` |
| **Sévérité par défaut** | Error |
| **Type** | Bug |
| **Unité du détecteur** | `uLockWithoutTryFinally.pas` |
| **Étiquettes** | `concurrency`, `lock`, `exception-safety` |
| **CWE** | [CWE-667](https://cwe.mitre.org/data/definitions/667.html) |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Acquiring a lock (TCriticalSection.Enter / .Acquire, TMonitor.Enter, TMultiReadExclusiveWriteSynchronizer.BeginWrite, WinAPI EnterCriticalSection) without immediately following with try..finally containing the matching Leave / Release / EndWrite is one of the most common Delphi threading bugs: any exception between the acquire and the release leaves the lock permanently held, deadlocking every subsequent caller. Lexical detection: matches the acquire patterns and checks whether the next statement after the `;` is `try`. Suppress with `// noinspection LockWithoutTryFinally` for intentionally-unprotected sequences (rare; usually means the next call is itself never going to throw).

## Exemples

### Mauvais (déclenche la règle)

```pascal
FLock.Enter;
DoWork;     // exception here leaves FLock held forever
FLock.Leave;
```

### Bon (correction idiomatique)

```pascal
FLock.Enter;
try
  DoWork;
finally
  FLock.Leave;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
