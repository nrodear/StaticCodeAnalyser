# SCA127 — Levée de la classe de base `Exception` au lieu d'une sous-classe

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `raise Exception.Create('...')` - la classe de base ne porte aucune information sémantique, les appelants ne peuvent pas filtrer sélectivement

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA127` |
| **Kind** | `RaisingRawException` |
| **Sévérité par défaut** | Warning |
| **Type** | Code Smell |
| **Unité du détecteur** | `uRaisingRawException.pas` |
| **Étiquettes** | `exception`, `error-handling`, `sonardelphi` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Raising the RTL base class `Exception` is the equivalent of throwing `new Error('something')` in JavaScript: it tells the caller *that* something went wrong, but not *what*. Calling code is then forced to catch the broadest possible class (`on E: Exception do ...`), which also catches every unrelated runtime error and tends to swallow bugs. Prefer a specific subclass (e.g. `EArgumentOutOfRangeException`, `EFileNotFoundException`, a domain-specific `EMyDomainError`). Detector walks every `nkRaise` node and flags it when the raised expression starts with `Exception.Create` (case-insensitive) or is exactly `Exception` (no Create call). Bare `raise;` (re-raise) and `raise E;` (variable) are not flagged. Maps to Sonar-Delphi `RaisingRawExceptionCheck`.

## Exemples

### Mauvais (déclenche la règle)

```pascal
if x < 0 then
  raise Exception.Create('x is negative');
```

### Bon (correction idiomatique)

```pascal
if x < 0 then
  raise EArgumentOutOfRangeException.CreateFmt('x = %d', [x]);
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
