# SCA091 — Instruction case volumineuse

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `case` avec 10 branches ou plus - envisager le polymorphisme ou une table de dispatch

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA091` |
| **Kind** | `CaseStatementSize` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uCaseStatementSize.pas` |
| **Clé de configuration** | `[Detectors] MaxCaseBranches` |
| **Étiquettes** | `complexity`, `open-closed` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

A `case` with many branches usually hides a polymorphism or strategy pattern. The code would be more open/closed (new behavior = new class, not new branch) and the file shorter when each branch becomes its own class method or a `TDictionary<Key, TProc>` entry. Matches SonarDelphi communitydelphi:CaseStatementSize. The threshold is configurable via `[Detectors] MaxCaseBranches` and defaults to 10. Every `case` is measured on its own: only labels at block depth 1 of that `case` count, so the labels of a nested `case` no longer inflate the enclosing one. A nested construct can therefore produce more than one finding - an outer `case` and an inner one are reported separately whenever each is over the threshold by itself.

## Exemples

### Mauvais (déclenche la règle)

```pascal
case Op of
  opA: ...; opB: ...; opC: ...; opD: ...; opE: ...;
  opF: ...; opG: ...; opH: ...; opI: ...; opJ: ...;
end;
```

### Bon (correction idiomatique)

```pascal
FOperations[Op].Execute;  // dispatch table
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
