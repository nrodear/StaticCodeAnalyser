# SCA193 — Caractère non-ASCII dans un identifiant

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Vulnerability-darkred)

> L'identifiant contient une lettre non-ASCII - risque d'homoglyphes et de caractères confondables

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA193` |
| **Kind** | `SourceNonAsciiIdentifier` |
| **Sévérité par défaut** | Warning |
| **Type** | Vulnerability |
| **Unité du détecteur** | `uSourceEncoding.pas` |
| **Étiquettes** | `security`, `unicode`, `trojan-source` |
| **CWE** | [CWE-1007](https://cwe.mitre.org/data/definitions/1007.html) |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

An identifier (variable, type, routine, ...) contains a non-ASCII character. This is the homoglyph / confusable-identifier vector of the Trojan Source family (CVE-2021-42694, CWE-1007): a letter such as Cyrillic U+043E looks like Latin 'o' but is a different identifier, so two identifiers can look identical yet bind differently. A legitimate Unicode identifier is also possible, so review before changing; prefer ASCII identifiers for portability and to avoid confusion.

## Exemples

### Mauvais (déclenche la règle)

```pascal
var Login: string;  // one letter is a Cyrillic homoglyph (U+043E), not Latin 'o'
```

### Bon (correction idiomatique)

```pascal
var Login: string;  // all-ASCII identifier
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
