# SCA087 — Section de visibilité vide dans la classe

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> En-tête de section `public`/`private`/... sans aucun membre

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA087` |
| **Kind** | `EmptyVisibilitySection` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uEmptyVisibilitySection.pas` |
| **Étiquettes** | `dead-code`, `refactor` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

A class with a visibility keyword followed immediately by another visibility keyword (or `end`) is usually a refactor leftover - all members of that section moved elsewhere. Delete the empty header. Matches SonarDelphi communitydelphi:EmptyVisibilitySection.

## Exemples

### Mauvais (déclenche la règle)

```pascal
type TFoo = class
  public
  private
    FX: Integer;
  end;
```

### Bon (correction idiomatique)

```pascal
type TFoo = class
  private
    FX: Integer;
  end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
