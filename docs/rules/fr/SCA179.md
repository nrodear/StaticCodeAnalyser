# SCA179 — Attribut DUnitX [Ignore] sans argument de justification

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> [Ignore] sans argument chaîne ignore le test silencieusement - ajouter un message expliquant pourquoi il est désactivé

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA179` |
| **Kind** | `AttributeIgnoreWithoutReason` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uAttributeIgnoreWithoutReason.pas` |
| **Étiquettes** | `test`, `dunitx`, `documentation` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

DUnitX accepts [Ignore] both as a marker attribute and as [Ignore('reason')]. The marker form skips the test without surfacing WHY it was disabled - the test then drifts unmaintained, often forgotten. Always add a reason string referencing a ticket / external dependency / known limitation so the disable can be triaged.

## Exemples

### Mauvais (déclenche la règle)

```pascal
[Ignore]
procedure SomeTest;
```

### Bon (correction idiomatique)

```pascal
[Ignore('TBD ticket #1234 - flaky on CI')]
procedure SomeTest;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
