# SCA074 — Assert sans message

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `Assert(cond);` - ajouter un message `'pourquoi'` pour le diagnostic

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA074` |
| **Kind** | `AssertMessage` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uAssertMessage.pas` |
| **Étiquettes** | `debuggability`, `errors` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

`Assert(cond)` alone fails with the generic 'Assertion failed at $address' - no clue what was actually expected. `Assert(cond, 'why')` saves debugging hours. Matches SonarDelphi communitydelphi:AssertMessage.

## Exemples

### Mauvais (déclenche la règle)

```pascal
Assert(Items.Count > 0);
```

### Bon (correction idiomatique)

```pascal
Assert(Items.Count > 0, 'caller must ensure non-empty Items');
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
