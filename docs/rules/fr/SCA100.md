# SCA100 — Alias de type pointeur sans préfixe P

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `Foo = ^Bar` devrait s'écrire `PBar = ^Bar` (convention du préfixe P)

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA100` |
| **Kind** | `PointerName` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uPointerName.pas` |
| **Étiquettes** | `naming`, `convention` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Delphi convention since day one: a pointer alias on `TXxx` is named `PXxx`. The `P` prefix makes the indirection visible at the call site. Matches SonarDelphi communitydelphi:PointerName. Naming framework will later make the prefix configurable.

## Exemples

### Mauvais (déclenche la règle)

```pascal
type TIntPtr = ^Integer;
```

### Bon (correction idiomatique)

```pascal
type PInteger = ^Integer;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
