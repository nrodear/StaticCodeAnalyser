# SCA076 — Déclaration groupée de variables, champs ou paramètres

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Séparer `A, B: Type` en une déclaration par ligne

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA076` |
| **Kind** | `GroupedDeclaration` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uGroupedDeclaration.pas` |
| **Étiquettes** | `style`, `diff-hygiene` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Grouped declarations (`A, B, C: Integer;` for vars, fields or parameters) confuse diffs (a new variable changes an existing line), prevent per-variable comments, and complicate type-change refactors. Each declaration on its own line is cleaner. Unifies SonarDelphi communitydelphi:GroupedFieldDeclaration / :GroupedVariableDeclaration / :GroupedParameterDeclaration into one rule (SCA may split later).

## Exemples

### Mauvais (déclenche la règle)

```pascal
var A, B, C: Integer;
field FX, FY: TPoint;
```

### Bon (correction idiomatique)

```pascal
var A: Integer;
    B: Integer;
    C: Integer;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
