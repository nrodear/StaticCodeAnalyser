# SCA156 — GetMem / AllocMem sans try/finally

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> GetMem / AllocMem / ReallocMem suivi du FreeMem correspondant dans la même routine mais sans try/finally englobant - le chemin d'exception perd le tampon définitivement

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA156` |
| **Kind** | `GetMemWithoutFreeMem` |
| **Sévérité par défaut** | Warning |
| **Type** | Bug |
| **Unité du détecteur** | `uGetMemWithoutFreeMem.pas` |
| **Étiquettes** | `memory-leak`, `exception-safety`, `mormot`, `sonar50` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Lexical scan (comments + strings stripped) for `GetMem(...)`, `AllocMem(...)`, or `ReallocMem(...)`. A 400-character lookahead window searches for the matching `FreeMem`. When both are present in the window but no `try` keyword precedes the FreeMem, the pair is reported - any exception between allocation and release leaks the raw heap buffer. mORMot-tuned: matches the ~20 GetMem occurrences in mORMot core/ that implement high-performance buffer manipulation, where every missing try/finally is a production leak. Patterns where FreeMem is missing entirely are skipped (likely ownership-transfer to caller, or custom allocator).

## Exemples

### Mauvais (déclenche la règle)

```pascal
GetMem(P, 1024);
DoStuff(P);
FreeMem(P);
```

### Bon (correction idiomatique)

```pascal
GetMem(P, 1024);
try
  DoStuff(P);
finally
  FreeMem(P);
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
