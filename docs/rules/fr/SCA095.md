# SCA095 — Section de visibilité répétée

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> La même section `public`/`private`/... apparaît deux fois dans une classe

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA095` |
| **Kind** | `ConsecutiveVisibility` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uConsecutiveVisibility.pas` |
| **Étiquettes** | `structure`, `style` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Two `private` (or `public`/`protected`/`published`) sections in the same class with members between them should be merged into one. Helps reviewers grasp class shape at a glance. Matches SonarDelphi communitydelphi:ConsecutiveVisibilitySection. Note: empty header followed by another header is the related but distinct `EmptyVisibilitySection` rule (SCA087).

## Exemples

### Mauvais (déclenche la règle)

```pascal
type TFoo = class
  private FX: Integer;
  public procedure A;
  private FY: Integer;
  end;
```

### Bon (correction idiomatique)

```pascal
type TFoo = class
  private
    FX, FY: Integer;
  public procedure A;
  end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
