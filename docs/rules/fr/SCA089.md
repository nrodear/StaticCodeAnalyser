# SCA089 — Champ public dans la classe

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Un champ public rompt l'encapsulation - utiliser une propriété

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA089` |
| **Kind** | `PublicField` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uPublicField.pas` |
| **Étiquettes** | `encapsulation`, `api-design` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

A public field exposes the underlying storage; callers can mutate it without the class noticing. A property gives the class the option to add validation/normalization later without breaking callers. Matches SonarDelphi communitydelphi:PublicField.

## Exemples

### Mauvais (déclenche la règle)

```pascal
type TFoo = class
  public
    Count: Integer;
  end;
```

### Bon (correction idiomatique)

```pascal
type TFoo = class
  private
    FCount: Integer;
  public
    property Count: Integer read FCount write FCount;
  end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
