# SCA132 — except on E: Exception attrape toutes les erreurs

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `on E: Exception do ...` attrape la classe de base et donc tous ses descendants, y compris les exceptions système qui ne doivent pas être étouffées (EOutOfMemory, EAccessViolation, EAbort)

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA132` |
| **Kind** | `ExceptionTooGeneral` |
| **Sévérité par défaut** | Warning |
| **Type** | Code Smell |
| **Unité du détecteur** | `uExceptionTooGeneral.pas` |
| **Étiquettes** | `exception-handling`, `error-handling`, `sonar50` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Catching `Exception` itself - the root of Delphi's exception hierarchy - silently swallows error categories the calling code is not prepared to handle: out-of-memory conditions, access violations, the `EAbort`-style flow-control exception, and anything else not derived from a domain-specific base class. Prefer a precise `on E: ESpecific do` clause, or several stacked clauses for the cases you can recover from. If you really need a fallback that re-raises after logging, use `except Log(''...''); raise; end;` - the bare `raise;` keeps the original type and stack. The detector walks `nkOnHandler` nodes and reports those whose `TypeRef` equals `Exception` (case-insensitive). Maps to Sonar-50 rule #11 (`ExceptionTooGeneral`).

## Exemples

### Mauvais (déclenche la règle)

```pascal
try
  ParseConfig(s);
except
  on E: Exception do        // swallows EOutOfMemory / EAbort / ...
    Log(E.Message);
end;
```

### Bon (correction idiomatique)

```pascal
try
  ParseConfig(s);
except
  on E: EConvertError do
    Log(E.Message);
  on E: EFileNotFoundException do
    Log(E.Message);
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
