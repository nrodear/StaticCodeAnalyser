# SCA078 — Capture générale sur la classe racine Exception

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> `on E: Exception do` avale tout, y compris les violations d'accès et l'épuisement mémoire

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA078` |
| **Kind** | `ExceptOnException` |
| **Sévérité par défaut** | Warning |
| **Type** | Bug |
| **Unité du détecteur** | `uExceptOnException.pas` |
| **Étiquettes** | `error-handling`, `bug-risk` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Catching the root `Exception` class catches every error including unrecoverable ones (EAccessViolation, EOutOfMemory, EStackOverflow). Prefer a specific exception type (EDatabaseError, EFOpenError, EConvertError, ...) so unrelated failures aren't masked. Suppress with `// noinspection` if the catch-all is intentional (e.g. logger boundary).

## Exemples

### Mauvais (déclenche la règle)

```pascal
try Foo; except on E: Exception do Log(E.Message); end;
```

### Bon (correction idiomatique)

```pascal
try Foo; except on E: EDatabaseError do Log(E.Message); end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
