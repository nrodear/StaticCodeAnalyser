# SCA027 — Liaison (DataSource, DataField) en double

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> Deux contrôles ou plus lient le même couple DataSource / DataField

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA027` |
| **Kind** | `DfmDuplicateBinding` |
| **Sévérité par défaut** | Warning |
| **Type** | Bug |
| **Unité du détecteur** | `uDfmDuplicateBinding.pas` |
| **Étiquettes** | `dfm`, `db-binding` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

When the user edits one bound control, the second receives a parallel update from the dataset - racey, hard-to-debug overwrites. Bind each (DataSource, DataField) to exactly one control.

## Exemples

### Mauvais (déclenche la règle)

```pascal
DBEdit1.DataField = 'NAME'
DBEdit2.DataField = 'NAME'   // same DataSource
```

### Bon (correction idiomatique)

```pascal
(bind 'NAME' to exactly one control)
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
