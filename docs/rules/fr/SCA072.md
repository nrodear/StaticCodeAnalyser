# SCA072 — Comparaison booléenne redondante

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `X = True` devrait s'écrire `X` (et `X <> False` de même)

## Métadonnées

| Champ | Valeur |
|---|---|
| **Identifiant de règle** | `SCA072` |
| **Kind** | `RedundantBoolean` |
| **Sévérité par défaut** | Hint |
| **Type** | Code Smell |
| **Unité du détecteur** | `uRedundantBoolean.pas` |
| **Étiquettes** | `style`, `bug-risk` |

## Description

> Cette section n'existe qu'en anglais. Le nom et la description courte sont traduits ; les textes complets suivent la version anglaise.

Comparing a Boolean to True/False is verbose and creates subtle bugs (WinAPI BOOL where Truthy != 1 evaluates `X = True` to false). The expression itself already is the condition. Matches SonarDelphi communitydelphi:RedundantBoolean. Note: `const X = True;` declarations are excluded (legitimate assignment, not comparison).

## Exemples

### Mauvais (déclenche la règle)

```pascal
if Active = True then ...
if Disabled <> False then ...
```

### Bon (correction idiomatique)

```pascal
if Active then ...
if Disabled then ...
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
