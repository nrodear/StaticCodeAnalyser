# SCA097 — Destructor without inherited call

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Bug-red)

> Destructor missing `inherited Destroy` - parent cleanup is skipped (leak risk)

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA097` |
| **Kind** | `DestructorWithoutInherited` |
| **Default severity** | Error |
| **Type** | Bug |
| **Detector unit** | `uDestructorWithoutInherited.pas` |
| **Tags** | `inheritance`, `memory-leak` |

## Description

A destructor without `inherited Destroy` (or `inherited;`) prevents the parent class from releasing its own fields, unregistering handlers, and updating reference counts. Almost always a leak. Matches SonarDelphi communitydelphi:DestructorWithoutInherited.

## Examples

### Bad (triggers the rule)

```pascal
destructor TFoo.Destroy;
begin
  FreeAndNil(FBar);
end;
```

### Good (idiomatic fix)

```pascal
destructor TFoo.Destroy;
begin
  FreeAndNil(FBar);
  inherited;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
