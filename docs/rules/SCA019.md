# SCA019 — TODO/FIXME marker in comment

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Open TODO / FIXME / HACK / XXX marker - resolve before release

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA019` |
| **Kind** | `TodoComment` |
| **Default severity** | Hint |
| **Type** | Code Smell |
| **Detector unit** | `uTodoComment.pas` |
| **Tags** | `work-tracking` |

## Description

Open work markers (TODO, FIXME, HACK, XXX, BUG, NOTE) live as a parallel work tracker inside the source. The pattern fails by accretion: markers added years apart never get triaged together, the original author leaves, and the next reader cannot tell whether the TODO is still relevant or has been silently shipped. SCA surfaces every such marker so CI gates can enforce a zero-TODO release branch or PR-time triage. Recognised forms: `// TODO`, `{ TODO ... }`, `(* TODO *)`, case-insensitive, with optional jira-id suffix (`// TODO PROJ-123: ...`). Marker tokens are configurable; the default set matches SonarDelphi.

## Examples

### Bad (triggers the rule)

```pascal
// TODO: handle the case where input is empty
```

### Good (idiomatic fix)

```pascal
if Input = '' then
  raise EArgumentException.Create('Input must not be empty');
Result := Parse(Input);

// Or, if it is not this change's job:
// TODO(#4711): handle empty input - see issue 4711
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
