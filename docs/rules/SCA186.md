# SCA186 — Invalid UTF-8 sequence in source file

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-File%20Error-grey)

> Malformed UTF-8 (overlong / surrogate / out-of-range) under a UTF-8 BOM

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA186` |
| **Kind** | `SourceInvalidUtf8` |
| **Default severity** | Error |
| **Type** | File Error |
| **Detector unit** | `uSourceEncoding.pas` |
| **Tags** | `encoding`, `corruption` |

## Description

The file carries a UTF-8 BOM but contains byte sequences that are not valid RFC 3629 UTF-8 (overlong encodings, lone surrogates, or code points above U+10FFFF). The Delphi compiler silently substitutes U+FFFD, corrupting the affected characters. Re-encode the file as clean UTF-8.

## Examples

### Bad (triggers the rule)

```pascal
(UTF-8 BOM present, but bytes contain an overlong or surrogate sequence)
```

### Good (idiomatic fix)

```pascal
(re-encode the file as valid UTF-8)
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
