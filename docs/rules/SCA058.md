# SCA058 — UPDATE / DELETE / TRUNCATE without WHERE

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Bug-red)

> SQL statement modifies every row - missing WHERE clause

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA058` |
| **Kind** | `SqlDangerousStatement` |
| **Default severity** | Error |
| **Type** | Bug |
| **Detector unit** | `uSqlDangerousStatement.pas` |
| **Tags** | `sql`, `data-loss` |
| **CWE** | [CWE-89](https://cwe.mitre.org/data/definitions/89.html) |

## Description

`UPDATE Users SET Active=0` without WHERE flips every row in the table. Same for `DELETE FROM ...` and `TRUNCATE TABLE ...`. Production-disaster waiting to happen.

## Examples

### Bad (triggers the rule)

```pascal
Q.SQL.Text := 'UPDATE Users SET Active=0';
```

### Good (idiomatic fix)

```pascal
Q.SQL.Text := 'UPDATE Users SET Active=0 WHERE Id=:id';
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
