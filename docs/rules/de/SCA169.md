# SCA169 — Assert-Argument enthält einen Aufruf mit Seiteneffekt

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> Assert(SomeCall) - der Aufruf entfällt im Release-Build und sein Seiteneffekt geht stillschweigend verloren

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA169` |
| **Kind** | `AssertWithSideEffect` |
| **Standard-Schweregrad** | Warning |
| **Typ** | Bug |
| **Detektor-Unit** | `uAssertWithSideEffect.pas` |
| **Schlagworte** | `reliability`, `assert`, `side-effect` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

The Delphi compiler removes Assert calls entirely from Release builds (unless DCC_Assertions is on). If the asserted expression contains a function call with side effects (allocation, state mutation, I/O, counter increment), the side effect disappears too - the Release-only behavior diverges from Debug. The detector flags Assert calls whose argument contains a function-call pattern, after excluding a whitelist of known-pure RTL helpers (Length, Assigned, SizeOf, IntToStr, ...). Move the call out of the Assert and assert on the result instead.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
Assert(InitializeSubsystem);   // Release build skips Init entirely
```

### Gut (idiomatische Korrektur)

```pascal
var Ok := InitializeSubsystem;
Assert(Ok, 'subsystem init failed');
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
