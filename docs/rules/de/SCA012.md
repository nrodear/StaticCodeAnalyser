# SCA012 — Methode überschreitet die Zeilengrenze

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Methode länger als das konfigurierte Maximum (Vorgabe 80 Zeilen)

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA012` |
| **Kind** | `LongMethod` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uLongMethod.pas` |
| **Konfigurationsschlüssel** | `[Detectors] LongMethodMaxBodyLines, LongMethodMaxStatements` |
| **Schlagworte** | `maintainability`, `complexity` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

Methods over ~80 statements are correlated with high defect density (Robert C. Martin, McConnell): the reader keeps context in working memory and once the method spills past one screen, that context overflows. Long methods also resist unit testing - test setup ends up duplicating internal state. Refactor: pull cohesive blocks into private helpers (ParseInput/Validate/Persist), or split per responsibility (data fetch vs. transform vs. emit). Threshold is configurable via [Detectors] LongMethodMaxBodyLines and LongMethodMaxStatements; lines are counted between the routine's `begin` and matching `end;`, comments and blank lines included. Compound-statement detection ignores nested anonymous methods so a method body that only contains a closure is not falsely reported.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
(150-line procedure doing parsing + validation + persistence)
```

### Gut (idiomatische Korrektur)

```pascal
(extract into ParseInput, Validate, Persist helper methods)
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
