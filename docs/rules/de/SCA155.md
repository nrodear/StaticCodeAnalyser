# SCA155 — with-Anweisung mit mehreren Zielen

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `with A, B do` (zwei oder mehr durch Komma getrennte Empfänger) macht die Member-Auflösung mehrdeutig - eine neue Methode an A oder B ändert stillschweigend die Bedeutung des Rumpfs

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA155` |
| **Kind** | `WithMultipleTargets` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uWithMultipleTargets.pas` |
| **Schlagworte** | `clarity`, `with-statement`, `sonar50` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

Regex match \bwith\s+<id>,\s*<id>\s*do\b across the whole file (multiline / comments stripped), followed by a bracket-balance pass over the with head. One target alone is already a code-smell (covered by fkWithStatement); two or more turn the body into a maintenance trap: a new method added to A or B can silently override what used to dispatch to the other. The compiler picks the closest match and never warns. Renames via the IDE refactor skip these references because the receiver is not named at the call site. mORMot-tuned: matches up to 200 characters per target name to tolerate fully-qualified names like Owner.SubObject.Component. Fix: split into separate with blocks, or - better - drop with entirely and qualify each member explicitly. Not reported: a single target whose commas sit inside an argument, index or type-argument list. Only a comma at bracket depth 0 separates targets; the balance pass counts round and square brackets and skips generic type arguments (TDict<K,V>), and string literals are blanked beforehand. If the head cannot be decided (no do at depth 0 in the window, unbalanced brackets) the finding stays - the filter may only remove, never add.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
with Form1, List1 do
  DoStuff;     // From Form1 or List1?
```

### Gut (idiomatische Korrektur)

```pascal
Form1.DoStuff;
List1.Sort;
```

## Absichtlich nicht gemeldet

Bei den folgenden Konstrukten schweigt der Detektor absichtlich. Jedes davon ist eine False-Positive-Klasse, die am Realwelt-Korpus gemessen wurde — keine Vermutung.

### A single target whose commas sit inside an argument, index or type-argument list

Only a comma at bracket depth 0 separates `with` targets. `with Obj.Method(A, B, 'C') do` has exactly one receiver - the commas belong to the call. The balance pass counts round and square brackets and skips generic type arguments (`TDict<K,V>`); string literals are already blanked, so a comma inside a literal can never split a head either. If the head cannot be decided (no `do` at depth 0 within the window, unbalanced brackets), the finding stays - the filter may only remove, never add.

```pascal
// not reported - one target, commas are arguments
with CL.AddInterface(CL.FindInterface('IFoo'), IBar, 'IBar') do
  RegisterMethod('...');

// still reported - two receivers
with Form1, List1 do
  DoStuff;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
