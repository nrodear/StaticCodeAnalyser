# SCA085 — X.Free; X := nil; sollte FreeAndNil(X) sein

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `FreeAndNil(X)` statt `X.Free; X := nil;` verwenden

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA085` |
| **Kind** | `FreeAndNilHint` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uFreeAndNilHint.pas` |
| **Schlagworte** | `idiom`, `safety` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

`FreeAndNil(X)` is the canonical Delphi idiom: it does both atomically and avoids a dangling pointer if `X.Free` raises (e.g. destructor failure). Manual two-line form is also more diff-noisy and easier to break apart during later refactors. Matches SonarDelphi communitydelphi:FreeAndNil.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
Obj.Free;
Obj := nil;
```

### Gut (idiomatische Korrektur)

```pascal
FreeAndNil(Obj);
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
