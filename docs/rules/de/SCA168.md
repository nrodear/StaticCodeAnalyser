# SCA168 — case-Anweisung ohne else-Zweig

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Die case-Anweisung hat keinen else-Zweig - unbehandelte Werte fallen stillschweigend durch

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA168` |
| **Kind** | `DefaultCaseInCaseStatement` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uDefaultCaseInCaseStatement.pas` |
| **Schlagworte** | `control-flow`, `default-case` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

A case statement without an else branch silently ignores all values that are not explicitly listed. When the case expression takes one of these unhandled values, no action is taken and no error is raised, which can mask logic bugs (especially when new enum values are added later and the case statements are not updated). Add `else ;` for an intentional no-op (which documents the decision) or a default handler that raises an exception / logs / asserts.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
case Status of
  stNew    : DoNew;
  stActive : DoActive;
end;
```

### Gut (idiomatische Korrektur)

```pascal
case Status of
  stNew    : DoNew;
  stActive : DoActive;
else
  raise EProgrammerError.Create('Unhandled status');
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
