# SCA185 — UTF-8-Quelldatei ohne BOM

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> UTF-8-Datei ohne BOM mit Nicht-ASCII - der Compiler liest sie als ANSI (Mojibake)

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA185` |
| **Kind** | `SourceUtf8NoBom` |
| **Standard-Schweregrad** | Warning |
| **Typ** | Bug |
| **Detektor-Unit** | `uSourceEncoding.pas` |
| **Schlagworte** | `encoding`, `bom`, `portability` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

A source file saved as UTF-8 without a byte-order mark and containing non-ASCII characters is read by the Delphi compiler as ANSI (system code page, GetACP) - producing mojibake in string literals and comments at runtime, and the result is machine-dependent. Fix: save as UTF-8 with BOM, or compile the project with --codepage:65001.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
// file saved as UTF-8 WITHOUT BOM, with non-ASCII text
ShowMessage('...non-ASCII...');  // compiler reads ANSI -> mojibake at runtime
```

### Gut (idiomatische Korrektur)

```pascal
// same file saved as UTF-8 WITH BOM (EF BB BF), or build --codepage:65001
ShowMessage('...non-ASCII...');
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
