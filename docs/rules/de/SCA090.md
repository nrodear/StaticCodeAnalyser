# SCA090 — Verschachtelter try-Block

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Verschachteltes `try..end` - das innere try in eine eigene Methode herausziehen

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA090` |
| **Kind** | `NestedTry` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uNestedTry.pas` |
| **Schlagworte** | `error-handling`, `complexity` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

Nesting try blocks makes the control flow of cleanup / error recovery harder to read. Most cases unwind by either re-ordering the cleanups or extracting the inner block into its own method. Matches SonarDelphi communitydelphi:NestedTry. The detector uses a depth heuristic (counting `try` vs `end` keywords) and may produce a few false positives in heavily nested begin/end code.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
try
  try
    DoStuff;
  finally
    Cleanup;
  end;
except
  Log;
end;
```

### Gut (idiomatische Korrektur)

```pascal
ProtectedDoStuff;  // helper wraps the inner try
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
