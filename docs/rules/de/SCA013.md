# SCA013 — Zu viele Parameter

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Methode hat mehr Parameter als konfiguriert erlaubt (Vorgabe 7)

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA013` |
| **Kind** | `LongParamList` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uLongParamList.pas` |
| **Konfigurationsschlüssel** | `[Detectors] LongParamListMaxParams` |
| **Schlagworte** | `api-design` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

High parameter counts are a leading indicator of an SRP violation: the routine is juggling concerns that should be split. Long lists also bring positional confusion (the third Integer is which one again?) and force callers to fill defaults that may not apply. Refactor: group cohesive parameters into a record (TOrderInput), promote configuration to a builder pattern (FluentQuery.Where(...).OrderBy(...)), or split the method by axis (SaveOrder + SaveOrderItems). Threshold via [Detectors] LongParamListMaxParams (default 5). out/var parameters count just like value parameters; the implicit Self for class methods does not count.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
procedure SaveOrder(id, customer, address, city, zip, country, total, tax, shipping: ...);
```

### Gut (idiomatische Korrektur)

```pascal
procedure SaveOrder(const Order: TOrder);
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
