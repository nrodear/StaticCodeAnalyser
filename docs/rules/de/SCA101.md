# SCA101 — Zweig ohne begin..end-Block

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `then`/`else`/`do <Anweisung>` - besser ein ausdrückliches `begin..end`

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA101` |
| **Kind** | `BeginEndRequired` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uBeginEndRequired.pas` |
| **Schlagworte** | `style`, `bug-risk` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

A single-statement branch without `begin..end` is easy to misread; adding a second statement later without re-blocking is a classic source of indentation-vs-semantics bugs. Style-debated rule (many Delphi codebases use the compact form deliberately) - lsHint, easy to disable in profiles. `else if` chain, `raise`/`exit`/`break`/`continue` and other block-opening statements are explicitly allowed. Matches SonarDelphi communitydelphi:BeginEndRequired.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
if Active then DoStuff;
```

### Gut (idiomatische Korrektur)

```pascal
if Active then
begin
  DoStuff;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
