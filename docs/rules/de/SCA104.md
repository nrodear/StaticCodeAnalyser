# SCA104 — Klassen-/Record-Typ ohne T-Präfix

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Klassen- und Record-Typaliase sollten mit `T` beginnen

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA104` |
| **Kind** | `TypeName` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uTypeName.pas` |
| **Schlagworte** | `naming`, `convention` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

Delphi convention: class types `TXxx`, records `TXxx` or `RXxx`, interfaces `IXxx`, pointers `PXxx` (SCA100), exceptions `EXxx`. This detector covers the class/record subset only. Forward declarations (`TFoo = class;`), `class of` references (`TFooClass = class of TFoo`) and generic syntax are handled. Matches SonarDelphi communitydelphi:TypeName.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
type Counter = class
  ...
  end;
```

### Gut (idiomatische Korrektur)

```pascal
type TCounter = class
  ...
  end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
