# SCA102 — Verschachtelte Routine in einer Methode

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Lokale verschachtelte Prozedur oder Funktion - auf Unit-Ebene herausziehen

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA102` |
| **Kind** | `NestedRoutine` |
| **Standard-Schweregrad** | Hint |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uNestedRoutines.pas` |
| **Schlagworte** | `modularity`, `testing` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

Nested routines (a `procedure`/`function` declared inside the local-decl section of another routine) are syntactically allowed but discourage testing and reuse: the inner routine can't be unit-tested or reused from elsewhere. Modern Delphi prefers anonymous methods (lambdas) for the rare cases the nesting was actually needed. Anonymous methods are NOT flagged (they have no name in the AST). Matches SonarDelphi communitydelphi:NestedRoutines.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
procedure Outer;
  procedure Inner; begin DoX; end;
begin
  Inner;
end;
```

### Gut (idiomatische Korrektur)

```pascal
procedure Inner; begin DoX; end;
procedure Outer;
begin
  Inner;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
