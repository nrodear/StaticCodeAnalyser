# SCA121 — Funktion weist Result nie zu

![Severity](https://img.shields.io/badge/severity-Error-red) ![Type](https://img.shields.io/badge/type-Bug-red)

> Der Funktionsrumpf endet, ohne `Result` (oder `<Funktionsname> := ...`) zu schreiben - der Rückgabewert ist undefiniert

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA121` |
| **Kind** | `RoutineResultUnassigned` |
| **Standard-Schweregrad** | Error |
| **Typ** | Bug |
| **Detektor-Unit** | `uRoutineResultAssigned.pas` |
| **Schlagworte** | `function`, `uninitialized`, `sonardelphi` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

A function that reaches its `end;` without any `Result := ...` or Pascal-style `<FunctionName> := ...` returns whatever register/stack garbage happens to occupy the result slot. In Release builds the caller therefore receives the value of the last same-sized call - a classic Heisenbug that flips with optimisation level. Detector is intentionally conservative: it skips functions that contain any `Exit` (could be `Exit(value)`) or `raise` (function may always throw). Note: the parser does record the `Exit(value)` argument, so this skip is coarser than strictly necessary - narrowing it would add findings and is therefore pending a measured corpus run. Procedures, abstract/forward/external declarations, and DispatchID-only declarations are also skipped. Partial coverage (Result set in one branch only) is currently a false-negative pending CFG analysis. Maps to Sonar-Delphi `RoutineResultAssignedCheck`.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
function GetCount(L: TList): Integer;
begin
  if L = nil then
    LogMessage('nil list');
  // Result never set -> garbage
end;
```

### Gut (idiomatische Korrektur)

```pascal
function GetCount(L: TList): Integer;
begin
  Result := 0;
  if L <> nil then
    Result := L.Count;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
