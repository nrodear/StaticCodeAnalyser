# SCA113 — TThread.Resume ist veraltet

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> `MyThread.Resume` - `MyThread.Start` verwenden (seit Delphi 2010)

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA113` |
| **Kind** | `ThreadResumeDeprecated` |
| **Standard-Schweregrad** | Warning |
| **Typ** | Code Smell |
| **Detektor-Unit** | `uConcurrencyExt.pas` |
| **Schlagworte** | `concurrency`, `deprecated` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

TThread.Resume has been deprecated since Delphi 2010 in favor of TThread.Start (suspends-on-create + resume-on-start became one explicit Start call). The compiler emits a deprecation warning, but it is easily missed in larger code-bases. Two fixes: (a) call .Start instead of .Resume, (b) construct with CreateSuspended := False and skip the Resume/Start step entirely. Detector matches any `<ident>.Resume` - if the identifier is NOT a TThread descendant (e.g. a TForm), suppress per line with `// noinspection ThreadResumeDeprecated`.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
MyThread := TWorker.Create(True);
MyThread.Resume;
```

### Gut (idiomatische Korrektur)

```pascal
MyThread := TWorker.Create(True);
MyThread.Start;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
