# SCA159 — Typisierter Exception-Handler mit leerem Rumpf

![Severity](https://img.shields.io/badge/severity-Warning-orange) ![Type](https://img.shields.io/badge/type-Bug-red)

> `on E: SomeException do ;` schluckt eine bestimmte Exception-Klasse stillschweigend - schlimmer als ein blankes `except end`, weil die Typangabe absichtsvoll aussieht

## Metadaten

| Feld | Wert |
|---|---|
| **Regel-ID** | `SCA159` |
| **Kind** | `EmptyOnHandler` |
| **Standard-Schweregrad** | Warning |
| **Typ** | Bug |
| **Detektor-Unit** | `uEmptyOnHandler.pas` |
| **Schlagworte** | `exception-handling`, `silent-failure`, `mormot`, `sonar50` |

## Beschreibung

> Dieser Abschnitt liegt nur auf Englisch vor. Übersetzt sind Name und Kurzbeschreibung; die Volltexte folgen der englischen Fassung.

Lexical scan for `on [E:] <ExceptionType> do (;|begin end)`. The typed `on E:` form gives the impression of considered handling - a reader assumes the developer thought about this specific exception. An empty body means the failure is invisible in production: no log, no UI feedback, no telemetry. Common mORMot review finding in cleanup code where 'this can be ignored' is not documented. The bare `except end` form is already caught by SCA-001 (EmptyExcept); this rule covers the typed-handler variant.

## Beispiele

### Schlecht (löst die Regel aus)

```pascal
try DoStuff; except
  on E: EDatabaseError do ;
end;
```

### Gut (idiomatische Korrektur)

```pascal
try DoStuff; except
  on E: EDatabaseError do
  begin
    Logger.Error(E.Message);
    raise;
  end;
end;
```

---

_Generated from [`rules/sca-rules.json`](../../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
