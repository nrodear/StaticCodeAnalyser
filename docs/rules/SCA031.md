# SCA031 — DFM component without published field

![Severity](https://img.shields.io/badge/severity-Hint-blue) ![Type](https://img.shields.io/badge/type-Code%20Smell-yellow)

> Component in DFM has no matching published field in the form class - stale DFM entry (schema drift)

## Metadata

| Field | Value |
|---|---|
| **Rule ID** | `SCA031` |
| **Kind** | `DfmSchemaMismatch` |
| **Default severity** | Hint |
| **Type** | Code Smell |
| **Detector unit** | `uDfmSchemaMismatch.pas` |
| **Tags** | `dfm`, `streaming` |

## Description

A named DFM component without a corresponding published field is a schema deviation, not a runtime error: the streamer resolves the field via FieldAddress, gets nil and silently skips the assignment (no EReadError). The component is still created but unreachable from code - a stale/dead DFM entry worth cleaning up.

## Examples

### Bad (triggers the rule)

```pascal
// .dfm contains object btnSave: TButton, but TForm1 has no btnSave: TButton field
```

### Good (idiomatic fix)

```pascal
(add the field to the published section of TForm1, or remove the stale component from the .dfm)
```

---

_Generated from [`rules/sca-rules.json`](../../rules/sca-rules.json) by [`tools/gen-rules-docs.py`](../../tools/gen-rules-docs.py). Do not edit by hand — re-run the generator instead._
